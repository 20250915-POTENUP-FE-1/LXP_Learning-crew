import { app, analytics, db, auth } from "./firebase.js";
import { API_ROUTES } from "./apiRoutes.js";
import { LECTURE_LIST_LIMIT } from "./config.js";

// firebase
export { app, analytics, db, auth };

// api routes
export { API_ROUTES };

// config
export { LECTURE_LIST_LIMIT };
