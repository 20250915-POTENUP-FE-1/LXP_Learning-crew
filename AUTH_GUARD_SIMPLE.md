# 인증 가드 문서

## 목적

로그인이 필요한 페이지를 자동으로 보호합니다.

---

## 현재 설정

### 보호된 경로 (로그인 필수)

```
/my → 로그인 필요 (미로그인 시 /login으로 이동)
```

### 공개 경로 (누구나 접근 가능)

```
/     → 메인
/main → 메인 페이지
/login → 로그인 페이지
/register → 회원가입 페이지
```

---

## 작동 원리

### 로그인한 사용자 → /my 접근

```
요청 → 쿠키 확인 → 토큰 있음 → 접근 OK
```

### 미로그인 사용자 → /my 접근

```
요청 → 쿠키 확인 → 토큰 없음 → /login으로 이동
```

### 로그인한 사용자 → /login 접근

```
요청 → 쿠키 확인 → 토큰 있음 → /로 이동 (로그인 페이지 필요 없음)
```

---

## 🎯 실제 예시

### 마이페이지 보호하기

```typescript
const authPaths = [
  "/login",
  "/register",
  "/mypage"
];
```

이제 `/mypage`는 로그인한 사용자만 접근 가능합니다.

---

## 핵심 정리

| 상황                   | 결과                   |
| ---------------------- | ---------------------- |
| 미로그인 + 보호 경로   | → /login으로 자동 이동 |
| 로그인 + 로그인 페이지 | → /로 자동 이동        |
| 로그인 + 공개 경로     | → 접근 가능 ✅         |
| 미로그인 + 공개 경로   | → 접근 가능 ✅         |

---
